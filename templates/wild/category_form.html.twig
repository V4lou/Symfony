{% extends 'base.html.twig' %}
{% block title %}Add a new category{% endblock %}

{% block body %}
    <div class="box-form-category">
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>

{% endblock %}